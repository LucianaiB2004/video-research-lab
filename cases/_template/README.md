# 案例标题

> 这是案例记录模板，不是已完成案例。复制到新的案例目录后填写，并删除本提示。

## 基本信息

| 字段 | 内容 |
|---|---|
| 案例编号 | `case-000` |
| 对应功能 | `01-10 中的一项或多项` |
| 状态 | `DRAFT / VERIFIED / FAILED` |
| 生成日期 | `YYYY-MM-DD` |
| 平台与模型 | `待填写` |
| 模型或服务版本 | `待填写；无法确认时写 UNKNOWN` |

## 目标

说明要复现、生成或改造什么，以及必须保留和允许变化的内容。

## 输入素材

| 编号 | 文件或链接 | 角色 | 权利或来源说明 |
|---|---|---|---|
| 视频 1 | `待填写` | `原视频 / 动作参考 / 运镜参考` | `待填写` |
| 图片 1 | `待填写` | `首帧 / 尾帧 / 新主体 / 场景 / 风格` | `待填写` |

## 使用的模板

- 模板路径：`待填写`
- 模板版本或文件哈希：`待填写`
- 选择原因：`待填写`

## 用户参数

```text
目标时长：
画幅：
必须保留：
允许调整：
禁止出现：
其他要求：
```

## 实际提交的完整提示词

```text
在这里保留实际提交给模型的完整内容，不要只保留摘要。
```

## 生成参数

| 参数 | 实际值 |
|---|---|
| 模式 | `T2VA / I2VA / FL2VA / Ref2VA / AUTO / 平台原始名称` |
| 时长 | `待填写` |
| 画幅或分辨率 | `待填写` |
| Seed | `UNKNOWN 或实际值` |
| 其他平台参数 | `待填写` |

## 输出结果

- 原始输出：`本地相对路径或可访问链接`
- 预览图：`可选`
- 未经后期处理的版本：`可选`
- 后期处理说明：`没有时写 NONE`

## 评估

| 维度 | 观察结果 | 结论 |
|---|---|---|
| 时间线与转场 | `待填写` | `通过 / 部分通过 / 失败 / 不适用` |
| 主体一致性 | `待填写` | `通过 / 部分通过 / 失败 / 不适用` |
| 动作与物理关系 | `待填写` | `通过 / 部分通过 / 失败 / 不适用` |
| 运镜与构图 | `待填写` | `通过 / 部分通过 / 失败 / 不适用` |
| 场景、风格与光影 | `待填写` | `通过 / 部分通过 / 失败 / 不适用` |
| 文字、标识与音频 | `待填写` | `通过 / 部分通过 / 失败 / 不适用` |

## 失败尝试

记录失败版本、问题现象、调整内容和调整后的变化。没有失败尝试时写 `NONE`。

## 已知限制

列出仍然存在的问题、无法确认的信息，以及该结果不能证明的内容。

## 复现步骤

1. 准备并按编号提交输入素材。
2. 使用上方记录的模板、完整提示词和参数。
3. 记录平台返回信息与原始输出。
4. 按相同评估维度复核结果。
